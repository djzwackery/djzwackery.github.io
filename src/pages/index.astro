---
/**
 * Default-locale (English) home page. Non-default locales are generated by
 * src/pages/[lang]/index.astro. Both render the shared Home component, which
 * reads Astro.currentLocale for translations.
 */
import Home from "../components/Home.astro";
---

<Home />
