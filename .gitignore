# build output
dist/
public/data/
# generated types
.astro/
# build-time content cache (YouTube feed + Twitch/7TV emote metadata & images; restored via actions/cache in CI)
.cache/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables (never commit real keys; keep the example)
.env
.env.*
!.env.example
# written from the YOUTUBE_COOKIES secret in CI; never commit a real one
youtube-cookies.txt

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# misc
.claude
.vscode